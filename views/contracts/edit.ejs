<!DOCTYPE html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Editar Contrato</title>
  </head>
  <body>
    <h1>Editar Contrato</h1>

    <form action="/contracts/<%= contract.id_con %>" method="POST">
      <label for="nombre_con">Nombre del Contrato:</label>
      <input
        type="text"
        id="nombre_con"
        name="nombre_con"
        value="<%= contract.nombre_con %>"
        required
      />
      <br />

      <label for="monto_con">Monto:</label>
      <input
        type="number"
        id="monto_con"
        name="monto_con"
        value="<%= contract.monto_con %>"
        required
      />
      <br />

      <label for="fecha_con">Fecha del Contrato:</label>
      <input
        type="date"
        id="fecha_con"
        name="fecha_con"
        value="<%= contract.fecha_con.toISOString().split('T')[0] %>"
        required
      />
      <br />

      <label for="clientid">ID del Cliente:</label>
      <input
        type="number"
        id="clientid"
        name="clientid"
        value="<%= contract.clientid %>"
        required
      />
      <br />

      <button type="submit">Actualizar Contrato</button>
    </form>
  </body>
</html>
